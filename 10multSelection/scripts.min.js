const checkboxes=document.querySelectorAll('.inbox input[type="checkbox"]');let firstChecked,lastChecked;function handleCheck(e){let index=0;e.shiftKey&&(checkboxes.forEach(checkbox=>{checkbox===this&&(void 0===firstChecked?(firstChecked=index,lastChecked=index):lastChecked=index),index++}),firstChecked>lastChecked&&([firstChecked,lastChecked]=[lastChecked,firstChecked]),index=0,checkboxes.forEach(checkbox=>{void 0!==firstChecked&&(checkbox.checked=index>=firstChecked&&index<=lastChecked),index++}))}checkboxes.forEach(checkbox=>checkbox.addEventListener("click",handleCheck));